<?php

namespace App\Utils;

trait ArrayUtils
{
    public static function mapping($source, $maps)
    {
        $keys = array_keys($maps);

        foreach ($source as $item) {
            # code...
        }
    }
}
